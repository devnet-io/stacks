# Foundation Stack guide

This Stack curates a layered way of building React and Cloudflare products. Shared libraries are preferred over product-local reinvention, but lower layers should not absorb product-specific behavior. Every agent must resolve target context before editing and must record meaningful progress checkpoints.

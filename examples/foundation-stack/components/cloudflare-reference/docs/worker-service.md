# Worker service reference

Keep request parsing, authentication, domain services, persistence adapters, and response mapping separate. Bind Cloudflare environment objects at the outer adapter boundary.

# D1 reference

Use migrations as the durable schema history. Keep SQL and row mapping behind a repository interface. Introspection and administrative browsing should be explicit tools rather than mixed into product request paths.

# Testing preferences

Use integration tests at filesystem, Git, and protocol boundaries. Keep unit tests for deterministic domain logic. Tests should explain the behavior being preserved rather than mirror implementation details.
